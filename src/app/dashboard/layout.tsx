export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  // this layout could include sidebar or other dashboard-specific wrappers in future
  return <>{children}</>;
}
